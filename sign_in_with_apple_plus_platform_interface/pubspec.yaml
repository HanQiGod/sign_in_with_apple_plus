name: sign_in_with_apple_plus_platform_interface
description: A common platform interface for the sign_in_with_apple_plus plugin.
homepage: https://github.com/HanQiGod/sign_in_with_apple_plus/tree/main/sign_in_with_apple_plus_platform_interface
version: 3.0.0

environment:
  sdk: ^3.3.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

  plugin_platform_interface: ^2.0.0

  meta: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
